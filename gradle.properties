# FIX #21: 4 GB — NDK + Kotlin + Compose + C++ JNI pueden causar GC-thrashing con 2 GB
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
# FIX #20: android.enableJetifier eliminado — todas las deps son AndroidX puro.
# Jetifier solo se necesita para dependencias antiguas de Support Library.
# Eliminarlo acelera el build.
kotlin.code.style=official
