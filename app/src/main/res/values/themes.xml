<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base para Compose puro. El proyecto no incluye Material Components
         como dependencia directa, solo Compose + AppCompat (transitivo desde
         activity-compose). Theme.AppCompat.DayNight.NoActionBar es lo que
         corresponde. Compose maneja toda la paleta en código (MaterialTheme). -->
    <style name="Theme.IVANNAFUSIONPRO" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>
